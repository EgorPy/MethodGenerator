type: container
layout: vertical
children:
- type: text_input
  bind: first_name
  props:
    placeholder: first_name
- type: text_input
  bind: last_name
  props:
    placeholder: last_name
- type: text_input
  bind: phone
  props:
    placeholder: phone
- type: text_input
  bind: email
  props:
    placeholder: email
- type: text_input
  bind: password
  props:
    placeholder: password
- type: button
  action: auth.register
  props:
    text: Submit
